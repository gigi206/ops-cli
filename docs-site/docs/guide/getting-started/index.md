---
sidebar_label: "Overview"
description: "Check the machine can host a cage, build the binary, run something inside one, and know where to look when it refuses."
---

# Getting started

Four pages, in order: check the machine can host a cage, build the binary, run
something inside one, and know where to look when it refuses. Fifteen minutes end to
end on a machine that already has the prerequisites.

- [Installation](installation): the static binary, a dev build, and what the install
  script places where.
- [Quick start](quickstart): your first sandboxed command, a project's toolchain, and a
  first agent launch.
- [`sbx doctor` and prerequisites](doctor): the kernel features and host tools a cage
  needs, and how to check them before the first launch rather than during it.
- [Troubleshooting](troubleshooting): the symptoms you are most likely to meet first,
  each pointing at the page that owns the fix.

Once something runs: the [how-to recipes](../how-to/) walk the three paths people
actually take, and [What sbx is (and is not)](../concepts/) states the reference class
the rest of the guide argues from.
